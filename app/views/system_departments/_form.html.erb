<%= form_with(model: system_department, local: true) do |form| %>
  <% if system_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(system_department.errors.count, "error") %> prohibited this system_department from being saved:</h2>

      <ul>
      <% system_department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :system_id, class: "govuk-label" %>
    <%= form.collection_select(:system_id, System.all, :id, :acronymn, {}, {class: "govuk-select"}) %>
  </div>

  <div class="field">
    <%= form.label :department_id, class: "govuk-label" %>
     <%= form.collection_select(:department_id, Department.all, :id, :name, {}, {class: "govuk-select"}) %>
  </div>

  <div class="actions">
    <%= form.submit class: "govuk-button"  %>
  </div>
<% end %>
